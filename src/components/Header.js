import React from 'react';
import '../styles/components/Header.styl';

const Header = () => {
  return (
    <header>
      <div>LOGO</div>
      <nav>MENU</nav>
      <div>HAM</div>
    </header>
  );
};

export default Header;
